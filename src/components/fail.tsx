// 'use client';

// import { useEffect, useState } from 'react';

function Fail() {
  // const [error, setError] = useState<boolean>(true);

  // if (error) {
  //   throw new Error('Fail');
  // }

  return (
    <>
      <h1>Hello</h1>
    </>
  );
}

export default Fail;
